# Klungkung Tourism (Next.js + Tailwind) — Prototype

## Cara menjalankan
```bash
npm install
npm run dev
```

Buka: http://localhost:3000

## Catatan
- Semua data bersifat dummy (lihat folder `data/`).
- Map menggunakan Google Maps embed (placeholder).
- File download dummy ada di `public/downloads/`.
