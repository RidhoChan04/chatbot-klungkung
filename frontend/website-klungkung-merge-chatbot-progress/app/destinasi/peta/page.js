import MapClient from "@/components/MapClient";

export const metadata = { title: "Interactive Map | Klungkung Tourism" };

export default function Page() {
  /* UI: interactive map entry */
  return <MapClient />;
}
